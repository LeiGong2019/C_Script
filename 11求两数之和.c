#include <stdio.h>
void main()
{
    int a, b, sum;
    a = 123;
    b = 456;
    sum = a + b;
    printf("sum is %d \n", sum);
}

/*
scanf和printf都是C标准的输入输出函数；
printf中，变量不要添加"&"符号；
一个C程序总是从main函数开始执行，无论main函数的位置在哪里；
每个语句或数据声明，最后一个必须为分号；
C语言
*/
